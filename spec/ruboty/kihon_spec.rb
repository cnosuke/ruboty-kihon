require 'spec_helper'

describe Ruboty::Kihon do
  it 'has a version number' do
    expect(Ruboty::Kihon::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
